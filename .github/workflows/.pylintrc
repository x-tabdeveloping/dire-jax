[FORMAT]
max-line-length=120  # Allow longer lines

[DESIGN]
max-locals=25        # Allow up to 25 local variables per function
max-statements=80     # Allow up to 80 statements per function
max-args=8           # Allow up to 8 arguments per function
max-positional-args=8 # Allow more positional arguments
